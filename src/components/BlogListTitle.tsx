import style from '@/styles/blogList.module.css';

// type Props = {
//   selectedTag: string;
// };

// export const BlogListTitle = ({ selectedTag, tags }: Props) => {
//   const matchingTag = tags.find((tag) => tag.id === selectedTag);
//   const tagName = matchingTag ? `"${matchingTag.tag}"の記事` : 'すべての記事';

//   return (
//     <>
//       <h1 className={style.blogListTitle}>{tagName}</h1>
//     </>
//   );
// };
